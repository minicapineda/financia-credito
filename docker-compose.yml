services:
  financia_credito:
    container_name: financia_credito
    restart: unless-stopped
    build:
      context: .
      dockerfile: Dockerfile.local
    ports:
      - 5170:5170
    volumes:
      - ./src:/app
      - /app/node_modules
